service:
  name: card-management-service
  port: 8105

database:
  url: ${DB_URL}

redis:
  url: ${REDIS_URL}

rabbitmq:
  url: ${RABBITMQ_URL}

security:
  jwt_secret: ${JWT_SECRET}

observability:
  metrics_path: /metrics
  health_path: /health

