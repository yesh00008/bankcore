service:
  name: compliance-aml-service
  port: 8109

database:
  url: ${DB_URL}

redis:
  url: ${REDIS_URL}

rabbitmq:
  url: ${RABBITMQ_URL}

security:
  jwt_secret: ${JWT_SECRET}

observability:
  metrics_path: /metrics
  health_path: /health

